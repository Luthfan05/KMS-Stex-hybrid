// Sidebars disabled — all content is now dynamic via Supabase PostgreSQL.
// Dokumen dikelola melalui tabel 'documents' dan 'document_versions' di Supabase.
import type { SidebarsConfig } from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {};

export default sidebars;
